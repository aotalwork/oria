<nav class="bg-white border-b border-gray-200">
  <div class="max-w-6xl mx-auto px-6 h-20 flex items-center justify-between">

    <!-- Logo -->
    <%= link_to authenticated_root_path,
                class: "flex items-center" do %>

      <span class="text-3xl font-bold text-blue-600 tracking-tight">
        ORIA
      </span>

    <% end %>


    <!-- Navegación -->
    <div class="flex items-center space-x-8">

      <%= link_to "Dashboard",
                  authenticated_root_path,
                  class: "text-gray-700 font-semibold hover:text-blue-600 transition" %>

      <%= link_to "Tareas",
                  tasks_path,
                  class: "text-gray-700 font-semibold hover:text-blue-600 transition" %>

      <%= button_to "Cerrar sesión",
                    destroy_user_session_path,
                    method: :delete,
                    form_class: "inline-flex",
                    class: "px-5 py-2 rounded-xl bg-gray-100 text-gray-700 font-semibold hover:bg-red-50 hover:text-red-600 transition" %>

    </div>

  </div>
</nav>